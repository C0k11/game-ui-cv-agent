原始发现 68 条，验证判决 36 条

 1 [high] ShopFlow 主动点开購買青輝石页 → money_popup 打断无条件 halt 整条链（gate 的 _NAV_SAFE 放宽根本够不着）
     facilities.py:156
     on_lobby 在大厅主动 tap `购买青辉石` 广告位进「購買青輝石」弹窗（免費組合包入口）。下一帧该页体内必然检出青辉石/组合包 ⇒ money.purchase_context() 非 None ⇒ pages.py:75 的 INTERRUPTS `money_popup` 命中，且它在

 2 [high] event 关卡列表每 tick 清 once:enter* → 进关的 once 保护是死码，列表滚动时会点到隔壁关
     event.py:142
     on_event_quest_list 开头无条件（不看 st.changed）弹掉所有 `once:enter*` / `once:sweep_enter*` 键，而同一次调用后面第 395 行才用 `self.pending(f"enter{ti}_{idx}")` 判『入场键已点过』——键刚被

 3 [high] runner 硬编码只跑 ui 模型，Flow.yolo 声明被忽略 → cafe 邀请 / schedule 全体课程表在自主跑里必然空转
     runner.py:125
     _observe 写死 detect.infer(frame, ("ui",))，而 cafe.py:43 和 schedule.py:23 都声明 yolo=("ui","avatar")。app/cli.py:98 的单步模式尊重 flow.yolo，主循环不尊重 ⇒ 同一条 flow『单步走得

 4 [high] 加成扫荡那一发的 AP 闸是 fail-OPEN：AP 读不出照样点掃蕩開始
     event.py:455
     on_stage_popup 的 bonus_sweep 分支写的是 `if ap_now is not None and ap_now < need:` ——read_topbar 返回 None 时整道闸被跳过，直接往下拉 MAX、点「掃蕩開始」。而这一页正是顶栏最容易读不出的场景（关卡面板/弹

 5 [medium] club/craft/shop 三个 flow 的 module 是 daily_routine，导致它们读的是不存在的 cfg 段，用户配置全部失效
     facilities.py:142
     base.py:60 `self.cfg = ctx.cfg.get(self.module or self.name, {})`，而 ClubFlow(29)/CraftFlow(70)/ShopFlow(142) 的 module 都是 "daily_routine"。config/schema

 6 [medium] 四个 flow 的 on_reward 用列表传 find = conf argmax，把「获得奖励」横幅当按钮点
     facilities.py:266
     observe.all() 一律按 conf 降序、find() 取第一个，所以 `obs.find([V.GOT_REWARD, V.CONFIRM], 0.40)` 里的书写顺序毫无意义，实际选的是 conf 最大的那个。battle.py:206-214 与 base.py:172-177 专

 7 [medium] mail / daily_mission / craft 的收工判据是单帧，没过 hold
     facilities.py:257
     base.py:251-266 的铁律是『任何“收工/没活了”的判断都必须过 hold(连续N帧)』，但 MailFlow.on_mail(257) 只要单帧检出 `一次领取灰色` 就 finish(CLEAN)；DailyMissionFlow(296) 单帧 `全部领取_灰色` 同样；Craft

 8 [medium] schedule / mining 在 stalled 分支里每帧自增区域/来源索引，几帧内跳过全部剩余分支
     schedule.py:127
     on_schedule_region 里 `if self.stalled(st,120): self.state["area_i"] += 1; ...; return self.exit_step(...)` —— stalled 判的是 st.frames_in_page>=120，退出 ta

 9 [medium] event 加成②的 sweep_i 从没被推进，多目标扫荡永远只扫第一个
     event.py:413
     `si = self.state.setdefault("sweep_i", 0)` 是全仓唯一一次出现 sweep_i（grep 确认无任何 += 或赋值），而 411-412 行注释声称『某关扫完（AP 不够/次数满）由 on_stage_popup 推进』——on_stage_popup 的 

10 [medium] EventFlow.on_battle_result 在 decide 期每帧 cleared+=1（battle.py 刚把同一个 bug 挪进 post）
     event.py:531
     `if self.state["phase"]=="clear" and self._bt()["seen_win"]: self.state["cleared"] += 1` 写在返回之前，结算页停留几十帧就 +几十——battle.py:189-198 为完全相同的 `unknown += 1`

11 [medium] stuck_frames 兜底对返回 wait() 的 flow 完全失效，卡死只能靠 15 分钟单 flow 上限兜
     runner.py:411
     主循环里 idle 只在 `act is None` 时自增（411），任何 flow 返回 wait(...) 都会把 idle 清零，于是 420 行的 `idle > stuck_frames(400)` 判据对『一直返回 wait』的局面永远不触发。而 flow 层大量终局分支正是无限 wa

12 [high] 扫荡面板上那道"贴着点击"的 AP 闸是 fail-OPEN，正是 08-09 近失的原型写法
     event.py:455
     `ap_now = R.read_topbar(obs, R.AP)` 后写的是 `if ap_now is not None and ap_now < need:`。read_topbar 用 `obs.find(cls, conf=0.25, region=(0,0,1,0.10))` 取锚点，

13 [high] last_solid 是「上一个页面」不是「当前底页」，退出游戏框的三层防线在大厅上同时失效
     machine.py:108
     `_last_solid` 只在页面**切换**时被赋成 `old`（machine.py:122-123），所以站在大厅时它等于「进大厅之前那一页」；且 runner.py:336 每条 flow 都新建 Machine，初值就是 "unknown"。于是 flow 刚开始、bot 在大厅上时 l

14 [high] income_context 用了几个常驻领取按钮当"收入语境"，导致 mail/craft/daily_mission/arena 页上金钱判据整体关闭
     money.py:57
     `_INCOME_MARKERS` 含 `CLAIM_REWARD_YELLOW/CLAIM_YELLOW/CLAIM_ALL_YELLOW/CLAIM_ONCE_YELLOW`，conf 阈值 0.35。这些不是「屏幕在给你东西」的信号，而是页面常驻按钮——pages.py:150-152 自己写

15 [high] arena 的"票数读不出→fail-closed"写在够不着的分支里，0 票照样点对手
     arena.py:66
     顺序是 62 行先 `rows = obs.rows(V.ARENA_ROW,...)`，63-64 行 `if rows: return tap_box(rows[0], "挑战最上面那个对手")`，66 行的 `if tix is None and self.stalled(st,60): re

16 [medium] LOBBY_NAV 列表里 NAV_RECRUIT 重复，页面层与金钱/返回键层对"是不是大厅"的判定带宽不一致
     vocab.py:49
     `LOBBY_NAV = [...NAV_SHOP, NAV_RECRUIT, NAV_RECRUIT]` 有两个 招募入口。pages.py:125 的 `Sig("lobby", any_of=V.LOBBY_NAV, min_any=3)` 走 Sig.match 的 `sum(1 for c

17 [medium] 免費組合包的購買键只按 cx 匹配、无行约束无 region，可能选中隔壁真钱包
     facilities.py:219
     `buys = obs.all(V.SHOP_BUY, 0.35)`（全屏，无 region），`same = min(buys, key=lambda b: abs(b.cx - free.cx))`，只校验 `abs(same.cx-free.cx) < 0.06`，**完全没有 cy/行归属校

18 [medium] sweep 的票数 fail-closed 同样只在"没有入场键"的分支里，0 票会去开面板
     sweep.py:127
     `_on_stage_list` 里 128 行的 `"票数读不出 → fail-closed，不出击"` 嵌在 120 行 `if not enters:` 里；只要列表上有入场键（正常情况恒有），tix 是不是 None 都会走到 143 行 `tap_box(target, "入场")`。re

19 [medium] sweep 的 AP 闸是一次性 once + fail-OPEN 双重放行
     sweep.py:150
     `if self.costs_ap and self.once("apcheck"):` —— `once()` 在**决策时**就把标记消耗掉（base.py:220-228 自己写明只给纯逻辑用），所以这道闸每次进面板只在**一帧**上生效；若那一帧顶栏被弹窗压暗，`read_topbar` 返

20 [medium] 余额投票凑不齐 3 票时台账永不 commit，kill-switch 全程不武装且 OCR 挂在每 tick 热路径
     ledger.py:85
     `due = (now - self._last_commit) >= self.sample_every`，而 `_last_commit` 只在 `_commit()` 内被更新；`_commit()` 只有在 `self._vote.settled([GUARDED])` 为真（同一个青辉石值

21 [low] Gate.allow 三道闸被元组一次性求值：被拦下的动作照样推进连发状态、照样跑一次 JIT 抓帧+推理、人审提前触发
     gate.py:215
     `for v in (self.money(...), self.dedup(...), self.jit(...)):` 是元组，Python 会在进入循环前把三个都算完。后果：①`money()` 里的人审回调 `self._ask(act, obs)` 先跑，随后 `dedup()` 可能把这

22 [low] ShopFlow.on_confirm_dialog 对所有页面的确认框硬编码 spend="信用点"，包括组合包页
     facilities.py:238
     `on_confirm_dialog` 是按 overlay 派发的（base.py:101-106），shop flow 在 lobby / shop / combo_pack / shop_pyroxene_tab 任何一页上弹出的双键框都会走到它，而它无条件 `tap_box(cf, "确认购

23 [high] runner 硬写 ("ui",) 忽略 flow.yolo —— 自主跑时 cafe/schedule 的 avatar 模型从没加载
     runner.py:125
     `_observe()` 写死 `detect.infer(frame, ("ui",))`，而 CafeFlow(cafe.py:43) 和 ScheduleFlow(schedule.py:23) 都声明了 `yolo=("ui","avatar")`；只有 app/cli.py:98 的 st

24 [high] 加成扫荡的 AP 闸 fail-OPEN，就贴在会弹「購買AP 💎30」的那一下点击上
     event.py:455
     `on_stage_popup` 的 bonus_sweep 分支写的是 `if ap_now is not None and ap_now < need:` —— AP 读不出(None)时闸整条跳过，紧接着就 `tap_box(sw, "扫荡开始")`(event.py:467)。而扫荡面板正是

25 [high] income_context 把「常驻领取按钮」当收入语境 → 大赛页/每日任务页上金钱闸全程失效
     money.py:73
     `purchase_context()` 第一段是 `if pyx is not None: if inc is not None: return None`，而 `_INCOME_MARKERS`(money.py:57-59) 里含 `领取奖励_黄 / 领取_黄 / 领取蓝色 / 全部领取_黄 

26 [high] profile.json 的 order 覆盖掉默认值 → momotalk / story_mining 的模块开关是假开关
     profile.json:17
     `_deep_merge` 对 list 是整体替换，profile.json 的 order 只有 9 项，把 DEFAULTS(schema.py:49-51) 里的 `momotalk`/`story_mining` 挤掉了；而 `registry.build()`(registry.py:4

27 [high] 换分支只加索引不导航 —— 悬赏/JFD 配置里的第 2、3 个分支永远进不去
     sweep.py:216
     `_next_branch_or_done` 把 `branch_i += 1` 之后返回的是 `wait("换分支: ...")`，没有任何回到分支选择页的动作。返回 wait 意味着 runner 认为「flow 有动作」，不会走 `_recover` 退出，于是画面仍停在**当前分支的关卡列表

28 [medium] 设施类 flow 用 frames_in_page<40 当收工判据 → 页面还没渲染完就报 CLEAN
     facilities.py:114
     craft(114)、shop(187)、mail(260)、daily_mission(299) 四处都是 `if st.frames_in_page < 40: wait` 否则 `finish(CLEAN, "...0 次")`。两个问题：①40 帧@30fps≈1.3s，而 config/s

29 [medium] MailFlow 从不点 `领取蓝色`，而 mail 页面身份正是靠它维持
     facilities.py:254
     `on_mail` 只找 `CLAIM_ONCE_YELLOW`，其次看 `CLAIM_ONCE_GREY` 判完成。但 pages.py:259-263 把 `领取蓝色` 加进 mail 签名时写明了理由：「领完一次后一次領取键消失、只剩逐条的蓝色領取键」。所以当邮箱里剩下只能逐条领的蓝键（或超过

30 [medium] cafe 邀请目标找不到时不留标记 → 咖啡厅与邀请列表无限乒乓到 flow 超时
     cafe.py:200
     `on_cafe_invite_list` 滑到 8 次仍没找到配置角色时，只 log+note 然后 `exit_step` 退出列表，**没有置 `invited_f{floor}` 或任何放弃标记**；退回 cafe 页后 on_cafe 第④步(cafe.py:110-115)的条件 `no

31 [medium] story_mining 只认 3 类来源，配置里的「羁绊剧情」被静默丢弃并回落成「全挖」
     mining.py:26
     `_SOURCE_CLS` 只有主线/短篇/支线三项，而 DEFAULTS(schema.py:133) 的 sources 含「羁绊剧情」、SCHEMA(schema.py:245-247) 的多选项还多出「活动剧情/後日談」。`on_story_hub`(mining.py:55-57) 先用 

32 [medium] 金钱步被人审拒绝后没有停手路径 —— 每 tick 重决策直到 flow 超时
     gate.py:111
     `Gate.money` 拒绝时返回 `Verdict(False, needs_human=True)`，既不 halt 也不 stop_flow；runner._dispatch(runner.py:260-269) 只认 `v.halt` 和 `v.stop_flow`，`needs_huma

33 [medium] bounty/jfd 的 difficulty="fixed" 未实现，静默退化成「打最上面那一关」（最低难度）
     sweep.py:142
     `_on_stage_list` 只处理 `== "highest"`，else 分支 `target = min(enters, key=cy)` 直接点屏上最靠上的入场键；`bounty.fixed_stage`(schema.py:57) 全仓无人读取。SCHEMA(schema.py:206

34 [medium] CraftFlow 的确认键花 19,000 信用点却没声明 money=True（同货币 shop 声明了）
     facilities.py:127
     `CraftFlow.on_confirm_dialog` 直接 `tap_box(cf, "确认")`，而这个框就是「確定製造 N 次」——老 brain/skills/craft.py:15/24 实测记录「快速制造 costs 信用点 only (probe: 19,000/unit)」「确认

35 [medium] 活动商店 auto_buy 全选全买，`event.shop.furniture` 开关无人读取
     event_shop.py:173
     `_scan_shelf` 里 auto_buy 为真就 `全部选择`→`批量购买`(event_shop.py:176-182)，货架上有什么买什么。DEFAULTS(schema.py:84) 里的 `event.shop.furniture: False` 全仓 grep 不到读取点，老代码里

36 [medium] 战术大赛商店 0 实现：arena 四个配置项全是死配置，arena_shop 页面无任何 handler
     schema.py:124
     `arena.level_diff / stop_at_rank1 / max_refresh / buy_energy_drink` 四个键全仓 grep 零读取点（stop_at_rank1 还在 SCHEMA:258 里当 toggle 吐给前端）。vocab 里 `ARENA_SHOP_TA

37 [medium] 分支页的 CLEAN 出口绕过票数对账，会在票没花完时报 CLEAN
     sweep.py:93
     `_on_branch` 在 `branch_i >= len(opts)` 时直接 `finish(Outcome.CLEAN, "配置的 N 个分支都跑过了")`，完全不看 `tickets0/tickets`；而同一 flow 的另一个出口 `_wrap`(sweep.py:221-233) 

38 [low] LOBBY_NAV 里 NAV_RECRUIT 重复，lobby 签名 2 个入口就能确认，与 back_key 的 ≥3 判据不一致
     vocab.py:49
     `LOBBY_NAV` 结尾是 `NAV_RECRUIT, NAV_RECRUIT`。pages.py:64-66 的 `hits` 是按 any_of **列表逐项**累加的，所以只要检出「招募入口」就贡献 2 分，再有任意 1 个入口即满足 `min_any=3` ⇒ 只有 2 个不同入口的转场

39 [low] event 报告里的「顶纪录 N 次」永远是 0（farmed 从未被写）
     event.py:471
     `setup` 初始化了 `farmed=0`，但全文件没有任何地方给它 +1（加成①用的是 `counter="bonus_enters"`，event.py:399）。于是 LEFTOVER 文案「顶纪录 {farmed} 次，扫荡 {swept} 次」恒为「顶纪录 0 次」，把真实进度掩盖掉 

40 [high] craft 缺「立即完成(券)」对话框拦截 —— 会花掉製造券
     facilities.py:80
     三个槽都在制造中、没有成品可领时，屏上「一次领取黄色」仍亮着。CraftFlow:80 把它当「领制造成品」点下去，游戏弹的是「要使用N個製造券立即完成嗎?」双键框 → 页面身份 confirm_dialog → CraftFlow:121 的 on_confirm_dialog 只判灰键，不灰就 

41 [high] arena 票数 fail-closed 被写在挑战分支之后，读不出照样出战
     arena.py:63
     arena.py:45-69 的顺序是：读票(46) → 只有读出来才判 <=0(47-53) → 攻击编制(58) → 对手行 rows[0] 直接挑战(63) → 最后才是「if tix is None and stalled: 票数读不出 → fail-closed，不出战」(65)。只要屏上

42 [high] read_ticket 缺「必须含 /」的分子证明，0 票会被读成分母
     read.py:278
     read_ticket 的三道校验全部依赖分母存在：cur>tot、tot>hard_max、cur>hard_max。当 OCR 把「0/5」左裁成 '5'（strip 左界吃掉分子，这是老代码 brain/skills/arena.py:208-216 记录的 2026-06-02 事故类型）时

43 [high] event 加成扫荡 sweep_i 永不推进，AP 全灌一关（老代码修过的 bug 原样复发）
     event.py:413
     event.py:411-430 的注释写着「扫荡也按推算目标逐个来…某关扫完由 on_stage_popup 推进」，但全仓 grep `sweep_i` 只有 413 行的 setdefault 和 425-430 的读取，**没有任何一处 +1**；on_stage_popup(448-475

44 [high] shop 的青辉石 tab 守卫挂在 train=0 的死类上，且丢了老代码的正向白名单
     facilities.py:168
     新代码三处依赖 `青辉石商店_已选择`：facilities.py:135 的红线注释、act/money.py:153-155 的 on_pyroxene_tab()、state/pages.py:248 的 Sig("shop_pyroxene_tab")。而这个 cls 训练 0 框、实战 0

45 [medium] 活动里程碑奖励（獎勵資訊）整期不领
     event.py:159
     新 event flow 只把 `奖励资讯` 当成「这个活动有没有活动点数通道」的探针读一下（event.py:155-168），从不点它。全仓 grep：EVENT_REWARD_INFO 只在 event.py:159 和 pages.py 的签名里出现；CLAIM_REWARD_YELLOW 

46 [medium] sweep 的「票数读不出→fail-closed 不出击」只在没有入场键时可达
     sweep.py:119
     sweep.py 文件头(10-14)写着「读不出 = fail-closed 不出击」，但 _on_stage_list 的实现顺序是：读票(107) → 只有读出来才判 <=keep(113-117) → `enters` 非空就滑动/入场(119-143)，而 `if tix is None:

47 [medium] momotalk 缺面板身份闸与逐行开启上限，会在别人的面板上替当前学生回复
     momotalk.py:79
     新 on_momo_chat(79-108) 只要屏上有 学生信息回复选项 就按策略点，从不校验「右侧会话面板现在是谁的」。老代码 brain/skills/momo_talk.py:503-525 有帧实锤的专门闸：点开学生 A 后右侧面板可能还停在 B（实测点了「贵音」，气泡头像全是 凯伊 cx

48 [medium] story_mining 只做节点层，缺篇/章节导航和已试节点去重
     mining.py:88
     新 on_story_nodes 只找 `剧情图标未完成` 行，找不到就滑 6 次换下一类。老代码 brain/skills/story_mining.py:431-491 的 _mine_action 是三层：①节点层（未完成图标同行的入场键）②章节层（内容区黄点 → 点它右侧 0.10 的章节标

49 [medium] schedule 丢了 ARROW_LEFT 区域轮转，只能到 5 个有 cls 的区
     schedule.py:88
     on_schedule_area 用 obs.cols(V.SCHOOL_AREAS) 列可选区域，而 vocab.py:97-98 的 SCHOOL_AREAS 只有 5 个（夏莱办公室/居住区/格黑娜/阿拜多斯/千年）。老代码 brain/skills/schedule.py:1356-1365

50 [high] income_context 全屏无 region，把「常驻领取按钮」当收入语境 → 整条金钱闸在 arena/mail/每日任务页被关掉
     money.py:64
     income_context 是 obs.find(_INCOME_MARKERS, 0.35) —— 无 region，全屏 argmax。_INCOME_MARKERS 里的 CLAIM_REWARD_YELLOW/CLAIM_YELLOW/CLAIM_BLUE/CLAIM_ALL_YELLOW

51 [high] 扫荡面板上的 AP 闸是 fail-OPEN，而 read_topbar 在这一页恰好读不出
     event.py:455
     `if ap_now is not None and ap_now < need:` —— 读不出(None)就整条闸跳过，直接往下走到 tap『掃蕩開始』。而这一页正是读不出的那一页：percept/read.py:292 的 read_topbar 用 conf=0.25 找顶栏锚点，同文件 e

52 [high] money 判据④要求数量步进 conf≥0.40，而同一族 cls 在弹窗上实测只有 ~0.26 → 最后一道防线永不触发
     money.py:108
     purchase_context 判据④(双键框内有数量步进器 = 购买/兑换框)用模块级 CONF=0.40 去 find([QTY_MAX, QTY_MAX_GREY, QTY_MIN, QTY_MIN_GREY, PLUS, PLUS_GREY, QTY_MINUS, QTY_MINUS_GR

53 [high] 青辉石 y 留白的绝对像素上限 42px 在 4K 上落进「吃进邻居」区间，会复现 08-07 的 18,036→18,003 误读
     read.py:65
     STRIP[青辉石] = (0.25, 5.5, 0.80, 30.0, 42.0)，icon_strip():172-173 用 pad_max_px 把 y 留白按绝对像素夹死。这组参数只在 **1440p** 标过(0.80×51.5px=41.2px，没触到上限)。换到 3840×2160：

54 [medium] 帧源死掉时 runner 的冻结探针/自愈/halt 全部够不着 —— 判据挂在「有新帧」之后
     runner.py:355
     主循环顺序是：wait_new → `if seq == self._last_seq: continue`(355-356) → ledger → **frozen_s() 死机探针(374)**。Feed.latest() 永远返回**保留的最后一帧**(feed.py:238-240，_fra

55 [medium] TOPBAR 判据三处定义、两个值；ledger 的采样前置闸用的是最严那个且失败无声
     read.py:100
     同一个『顶栏』判据定义了三份且不一致：percept/read.py:100 TOPBAR=(0,0,1,**0.10**)、state/pages.py:32 TOPBAR=(0,0,1,**0.12**)、act/money.py:141 又写了一份内联字面量 (0,0,1,0.12)。act/

56 [medium] arena 的「票数读不出→不出战」fail-closed 写在对手行分支之后，永远够不着
     arena.py:65
     on_arena 的顺序是：46 行读票(read_ticket 失败返回 None) → 62 行 rows = obs.rows(ARENA_ROW, 0.40, region) → **63-64 行 if rows: return tap_box(rows[0], 挑战最上面那个对手)** 

57 [medium] feed 的静止探针绕开 Device.screencap，热路径 ADB 锁被 1.6s 的 4K 抓帧周期性占住且不留日志
     feed.py:264
     _is_static() 直接 subprocess 跑 `adb exec-out screencap -p`(264-268)，没走 percept/device.py:165-183 的 Device.screencap —— 那里承诺『每次调用都打日志，如果你在热路径日志里看到它那就是 bu

58 [low] 死机判据两处不一致：feed 说要「静止+tap无响应」，runner 只用内容静止就 halt，step_mode 人审会误判
     runner.py:375
     percept/feed.py:230 的 frozen_s() docstring 定的判据是『>30s **且 tap 无响应** ⇒ 疑似游戏死机』，但 runner.py:374-392 只看内容静止时长(>120s)，第一次重建 feed、第二次直接 halt 整轮并写 '疑似游戏死机（需

59 [low] vocab.require() 只在测试里被调用，A11 死判据闸在生产代码里完全没接线
     vocab.py:323
     require() 的设计意图是『在 import 期就把拿死类当唯一信号打回来』(vocab.py:5-13/329-331)，但全仓 grep 只有 tests/test_offline.py:627,632 调过它，flow/ 和 state/pages.py 一处都没有 ⇒ 这道闸对真实判据

60 [high] last_solid 卡在 "unknown" 时，退出游戏框的**两道**防线同时 fail-OPEN
     machine.py:108
     `_last_solid` 初值 "unknown"，且只在**页面发生切换**时才被赋成 old page（machine.py:122-123）。若 Machine 建好时屏上就是一个双键框，classify 给 page="unknown"，`tail[0] != self._page` 永远

61 [high] _LOBBY_LIKE 含 "facility"，把设施页弹出的游戏内双键框判成退出框并点取消
     machine.py:44
     pages.py:110-112 自己写明判据是「屏上有 返回键/回大厅 = 在设施页里 = **游戏内**对话框」，但白名单却把 `facility` 放了进来（money.py:117 同一份常量复制了第二遍，§A2 违规）。`facility` 是常态页：schedule.py:28 的 `o

62 [high] 剧情「跳過」确认框被 quit_dialog(优先级95, instant) 抢走 → 剧情逃生链永远走不完
     pages.py:99
     quit_dialog 签名 = CONFIRM+CANCEL 且顶栏货币/BACK/HOME 都没检出，而剧情过场里顶栏和返回键**本来就没有**，跳過确认框恰好只有這兩个键 ⇒ 完全命中。它 priority 95 > story_cutscene 90 且是 instant，story_cut

63 [medium] LOBBY_NAV 里 NAV_RECRUIT 重复，`lobby` 的 min_any=3 只要 2 个不同 cls 就成立
     vocab.py:49
     `LOBBY_NAV` 共 9 项但只有 8 个不同值（NAV_RECRUIT 写了两遍），而 Sig.match(pages.py:64-66) 是 `for c in self.any_of` **按列表逐项数**，重复项被计两次。实跑验证：`classify(O(B(招募入口), B(商店入口

64 [medium] event_ended 唯一信号是 train=32/val=0 的弱类，且真正强的 `距离奖励获得结束` 全仓没用
     pages.py:190
     `event_ended` 的 any_of 只有 EVENT_AFTERSTORY，HEALTH 表记的是 (32, 0)（vocab.py:294）—— 样本薄且 val 一框都没有，从没被验证过。它是 event flow 唯一的"我进错到上期活动了"出口（event.py:96 on_eve

65 [medium] battle_result 的 forbid 漏掉半个领取族，邮件/每日任务页被判成战斗结算
     pages.py:137
     forbid 只列了 CLAIM_ONCE_YELLOW / CLAIM_ONCE_GREY / CLAIM_ALL_YELLOW / NAV_CAFE，漏了 CLAIM_ALL_GREY、CLAIM_BLUE、CLAIM_YELLOW/GREY、CLAIM_REWARD_*。实跑验证：`O(全部领

66 [medium] EventFlow.on_battle_result 的 cleared 每 tick +1（数 decide 调用次数不是场次）
     event.py:531
     `if self.state["phase"]=="clear" and self._bt()["seen_win"]: self.state["cleared"] += 1` 写在 decide 路径里，既没有 post 也没有 once。结算页会持续存在很多 tick（要等 確認 被点掉并连续 

67 [medium] overlay 确认无迟滞：取消键闪一帧就把 confirm_dialog/ack_dialog 打成永久 None，且 flapping 检测看不见
     machine.py:90
     `_overlay` 要求最近 confirm 帧**完全相同**才成立，一旦不同立刻置 None（没有粘滞）。而 confirm_dialog 与 ack_dialog 的区别**只有一个 CANCEL**（pages.py:291 vs 309），取消键 conf 掉到 0.45 以下的那一帧身

68 [low] blank 逃生预算 6 次数的是 tick 不是落地 tap，6 帧内烧光后本 flow 再不恢复
     runner.py:183
     `return a if (a is not None and self.state_blank_taps() < 6) else None` —— state_blank_taps()(runner.py:72-74) 在**决策路径**里自增，每个 blank tick 都涨一次，与 tap 是
