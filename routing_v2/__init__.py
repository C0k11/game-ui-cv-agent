# -*- coding: utf-8 -*-
"""routing_v2 —— 碧蓝档案日常路由的整体式状态机（2026-08-08 重写）。

老的 `brain/skills/` 是 13 个各自为政的 skill，判据复制粘贴、兜底互相打架。
本包把整条链拆成 6 层，每层只做一件事，**判据只有一个定义处**：

    config/   用户配置（前端可选项）—— 纯数据
    percept/  帧  结构化观测（scrcpy 0ms / YOLO / 数字读取）
    state/    观测  页面身份（结构判据 + 连续 N 帧确认）
    act/      动作 + 三道闸（金钱 / 落地复验 / 连发）
    flow/     每个玩法一个 flow：「看到 X 就做 Y」
    app/      主循环 + CLI + 前端 API

设计铁律（每条都对应 README 里一条实测事故，改代码前先读 README）：
  A1  兜底默认必须是 **no-op**。破坏性动作要有专属 cls 支撑。
  A2  页面判据只在 `state/pages.py` 定义一次。
  A3  状态必须**连续 N 帧**一致才生效。
  A4  帧源只用 scrcpy；ADB 只用来 tap。
  A5  零 wait；靠"下一步 cls 出现"推进。
  A6  为模型缺陷写的兜底要登记，缺陷消失后自动失效并告警。
  A7  已知失效模式加闸，不许赛跑。
  A8  坐标只能从当帧 cls 推导。
  A9  页面身份只看结构，不看"有几个 / 打过没"。
  A10 数值判据挂人眼真值标定。
  A11 判"缺数据"前先确认不是废案（`_废弃` 前缀在 detect 层直接丢弃）。
"""
__all__ = ["percept", "state", "act", "flow", "app", "config"]

__version__ = "2.0.0"
