# 任务 (Tasks / 任務) — interactive probe log

探测日期 2026-06-01 · 纯-YOLO probe (scripts/probe.py) · ui=v5 · 用户手动遥控

目的：任务板块（每日/每週/成長 任务领取）点击链路 → 干净重写 skill。

★ 全局教训沿用：① ADB tap 偶发丢点 → 关键 tap「点→验证→重试」；
  ② GOT_REWARD dismiss 点头部框(top)，别点中心；③ 浮层坐标落卡身。

---

## Step 1 — 大厅，点任务大厅入口
帧 2363x1331

| cls | center | conf | 备注 |
|---|---|---|---|
| 任务大厅入口 | (0.953, 0.832) | 0.99 | 任務 入口（右下）|

dot 状态：附近红点 (0.855,0.712) / (0.976,0.698) → 可能有可领任务。

→ 下一步（用户指令）：点 `任务大厅入口` (0.953, 0.832) 进入任务

---

## Step 2 — 任務 内容大厅（游戏模式 hub，非每日任务领取）
帧 2363x1331 · 21 cls · 点 任务大厅入口(0.953,0.832) → 进 任務 hub

布局（标题 任務，左上 返回键）：
- 大 tile：**任務**(Area 30 第30任務 = 主线推图区) / **劇情**(0.813,0.242)
- 小 tile：`悬赏通缉`(0.560,0.548) / `總力戰` / `綜合戰術考試` / `大決戰` /
  `特殊任务`(活動進行中, 0.549,0.676) / `学院交流会`(0.545,0.803) /
  `战术大赛`(0.685,0.803) / 制約解除決戰(活動進行中, Clear)
- `任务关卡推图`(0.598,0.242) = 主线 tile 的 cls
- 红点(0.111,0.179) / 黄点 ×多（各 tile）/ `距离结束还剩`(事件计时)

★ 这是【战斗/内容 hub】，含 **campaign_sweep / bounty / arena 等已有 skill 的入口**。
⚠️ **不是「每日任务奖励领取」屏**。每日任务领取(全部领取)可能在别处（待用户指认入口）。
⚠️ 特殊任務 / 制約解除決戰 = **活動進行中**（用户规则：不测活动）。

→ 待用户明确：A) hub 里某模式  B) 每日任务领取(别处入口?)  C) 探 hub 导航
用户：先去 **悬赏通缉**（bounty）。

---

# === 悬赏通缉 (Bounty) 分支 ===
（bounty.py 已存在；本次 probe 重点：根治"票=0 买票花青辉石"bug + 验证入口/扫荡流程）

## Step 3 — 悬赏通缉 Location Select（分支选择）
帧 2363x1331 · 11 cls · 点 `悬赏通缉`(0.560,0.548) → 进 bounty Location Select

- **持有票券 1/6**（top-left）← `悬赏通缉票` cls(0.053,0.140)=票图标；"1/6" 数字在其右
  ★★ **digit-OCR 读票数**：region ~(0.06,0.10,0.17,0.18) 估 → parse "X/6"。
     **票=0 绝不 sortie**（根治买票bug：0/6 还出击→弹購買票券青辉石框）
- 标题 懸賞通緝 / Location Select（選擇委託，各地區學園增益效果）
- 3 分支 tile（右侧竖排）：
  - `高架公路`(0.903,0.248) — 桃葉媚/山海稀… 战术BD↑+技术笔记
  - `沙漠鐵道`(0.907,0.408) — 三一/西曼妮…
  - `教室`(0.927,0.566) — 千年/阿琪多斯/赤土/狂… ← 用户 app_config bounty_branches 设的
- 分支按要 gear 的学生选；profile.bounty_branches 配置

⚠️ 买票陷阱根治：入口先 OCR 票数，0/6 → 直接 exit，绝不进 sortie。

→ 下一步（待用户）：进哪个分支（教室?）→ 探 sortie→battle→reward
★ 用户：**分支由 dashboard 前端选项决定**（profile.bounty_branches）。本次 = 教室。
  skill 读配置 → 在 Location Select 里点对应分支 tile（高架公路/沙漠鐵道/教室）。

## Step 4 — 进 教室 分支（dashboard 选的）
→ 点 `教室`(0.927, 0.566) 进入

---

## Step 5 — 教室 关卡难度列表（關卡目錄）
帧 2363x1331 · 19 cls（稳定帧）

- 懸賞通緝票券 **1/6**（`悬赏通缉票` cls 0.094,0.271）
- 关卡列表（竖排，05~10 **被襲擊的教室 E~J**，数字↑ = 难度↑）：
  - 6 行 `关卡得星_3`（x≈0.561，y=0.211/0.349/0.488/0.626/0.765/0.903）= 全 3 星(已通关)
  - `入场键`（x≈0.873，y=0.327/0.465/0.603/0.742/0.880）对应各行
- **最高难度 = 列表底部**（stage 10，入场键 ~0.873,0.880）；列表可滚动(可能有更高)
- 學園增益：阿拜多斯/赤冬/千年/狂

★ skill 策略：跑**最高可通关难度**（每票奖励最大）→ 选最底/最高 `入场键`（或滚到底）。
  难度可配 dashboard（默认最高）。
★ `关卡得星_3` cls = 该关 3 星（已通关，可刷）。

→ 下一步（待用户）：进哪个难度（最高 stage 10 = 入场键 0.873,0.880?）
★ 用户策略：用 `关卡得星_3` + `入场键` bbox **配对**确定可交互关卡行（同 y=一关）→
  滑动确认底部无更高难度 → 选**最底** `入场键`。纯 YOLO，无硬编码关卡位。

## Step 6 — 向上滑确认底部无更高难度
→ swipe (0.7,0.78)→(0.7,0.32) 列表内容上移，看 stage 10 下面有没有 11+

实测：滑后 05 滚出顶部，底部**仍是 stage 10(教室J)**；入场键/星 bbox 仅移 ~0.01 = **触底**。
**无 11+ 更高难度。** stage 10 = 教室分支最高难度。

★ **skill 判到底**：滑动后若 `入场键` bbox 集合位置几乎不变（Δy < 阈值，如 0.03）= 到底，
  停止滑动 → 选**最底**（max y）`入场键`。否则继续滑。

## Step 7 — 选最底（最高难度 stage 10）入场
帧 · 滑后 `入场键` y=0.317/0.455/0.593/0.732/**0.870**，最底=0.870=stage 10
→ 点最底 `入场键`(0.873, 0.870) 进 stage 10（被襲擊的教室J）

---

## Step 8 — 任務資訊 弹窗（stage 10：掃蕩 / 任務開始）
帧 2363x1331 · 15 cls · 点 入场键 → 弹「任務資訊」

- 左：10 被襲擊的教室J 推薦 Lv.75；敵人/相性；任務目標(完成/全員生存/150秒內)；
  獲得期待獎勵（装备素材 + 技术笔记，⊕×10 等一排）
- 右 **掃蕩** panel：
  - 数量 `MIN_灰色`(0.615,0.414) `减号灰色`(0.669,0.414) [1] `加号灰色`(0.792,0.414) `MAX_灰色`(0.845,0.414)
    —— **全灰=只 1 票锁数量 1**（多票时可调）
  - `扫荡开始`(0.730,0.560) 票 **1 → 0** ← 3星可扫，瞬间领奖不打 ⭐日常用这个
  - `任务开始`(0.730,0.744) 票 1 → 0 ← 实战出击
- 通關部隊(0.16,0.13) = 预设队伍；`弹窗叉叉`(0.880,0.192)

★★ **bounty 日常 = `扫荡开始`**（瞬间，耗 **悬赏通缉票**，**非青辉石** ✅）。
   多票时先 `MAX` 拉满一次扫光；本次只 1 票（锁 1）。扫完 1→0 → bounty done。
★ skill 逻辑：票>0 → 入场最高难度 → 掃蕩 MAX → `扫荡开始` → reward dismiss → 票=0 done。
   票=0 入口就 skip（根治买票bug）。`MAX_灰色/MIN_灰色` cls 判数量边界。

→ 下一步（用户确认）：点 `扫荡开始`(0.730,0.560) 扫这 1 票

★★ 用户澄清【MAX 量态】：**正常流程 = 检到 `MAX_可点击` → 点 MAX 拉满 → 扫荡开始**。
   本次只 1 票 → `MAX_灰色`（不可点）→ 跳过 MAX，直接 扫荡开始。
   skill 判别：`MAX_可点击` 在 → 先点它拉满；`MAX_灰色` 在 → 跳过（已锁/1票），直接扫荡。

## Step 9 — 扫荡开始（本次跳过MAX，因只1票）
→ 点 `扫荡开始`(0.730, 0.560)

实测：点 扫荡开始 → 弹「通知」确认框：
**「要使用 1 懸賞通緝票券 掃蕩 1次 嗎？」**
- `确认键`(0.597,0.697) / `取消键`(0.400,0.700) / `弹窗叉叉`(0.691,0.229)
- 消耗 = **懸賞通緝票券**（非青辉石 ✅）

★★★ **防青辉石关键判别点（bounty 买票bug 根治）**：
   扫荡确认框文字/图标 = 「懸賞通緝票券」→ 安全，点 `确认键`。
   若文字/图标变成「青輝石」(票=0 时游戏改提示买票)→ **立即 `取消`/`弹窗叉叉`，绝不确认**。
   （三道防线：① 入口 OCR 票数 0 则不进 ② 不点 立即完成/买票 ③ 此确认框验"票券"非"青辉石"）

→ 下一步（用户确认）：点 `确认键`(0.597,0.697) 完成扫荡

---

## Step 10 — 確認 → 扫荡成功，票 1→0（DONE 信号）
帧 2363x1331 · 13 cls · 点 `确认键`(0.597,0.697) → 扫荡完成，回 任務資訊

- 顶部 mission banner「完成懸賞通緝20次 **14/20**」↑（扫荡计数 +1，确认生效）
- 掃蕩 panel 现在：数量 **0**（`MIN_可点击`0.615,0.414 出现；`加号灰色`0.792 / `MAX_灰色`0.845），
  票指示 **0 → -**
- **票数已 0**（用掉最后 1 票）= bounty DONE
- **无独立 获得奖励 弹窗**（bounty 扫荡奖励直接入库；靠 mission banner 提示）

★★ **0 票 DONE 信号 + 安全**：掃蕩 数量=0 / 票"0→-" / `MAX_灰色`+`加号灰色`。
   **此时绝不点 掃蕩開始/任務開始**（0 票点了 → 弹买票青辉石框）！skill 检票=0 → exit。

═══════════════ 悬赏通缉(bounty) 流程探明（Step 3-10）═══════════════
- 入口    : 任務hub → `悬赏通缉`(0.560,0.548) → Location Select
- 票检查  : digit-OCR 持有票券 X/6；**0 → 直接 exit**（防买票，防线①）
- 分支    : dashboard profile.bounty_branches（本次 教室 0.927,0.566）
- 选关    : `关卡得星_3`+`入场键` bbox **配对**成行；滑到底(bbox Δy<阈值)→选最底=最高难度入场键
- 出击    : 任務資訊 → `MAX_可点击` 在则点 MAX(多票一次扫)，否则跳过 → `扫荡开始`(0.730,0.560)
- 确认    : 「要使用 N 懸賞通緝票券 掃蕩」→ **验"票券"非"青辉石"** → `确认键`；是青辉石 → 取消(防线③)
- 完成    : 票 1→0，mission 计数+1，奖励直接入库（无 reward 弹窗）；票=0 → exit
- ⛔ 绝不  : 票=0 点 掃蕩/任務開始（防线②）；确认框见 青辉石 还确认
- 判别cls : `悬赏通缉票` / `关卡得星_3` / `入场键` / `扫荡开始` / `任务开始` / `MAX_可点击·灰色` / `MIN_可点击·灰色`
═══════════════════════════════════════════════════════════

→ 下一步（待用户）：bounty 已扫完(0票)，退出 → 回 hub/大厅

### ⚠️ 修正：扫荡确认后【有 掃蕩完成 奖励弹窗】（probe 漏截，用户截图纠正）
我 Step10 那帧（任務資訊+0票）是 stale/过渡帧，**实际确认后先弹「掃蕩完成」奖励弹窗**：
- 标题 **掃蕩完成** / Cok11 Lv.90 MAX
- **1次獲得獎勵** + **最終獲得獎勵**（各一排：⊕×10 / 装备 / 技术笔记 / 强化石 / ×36K 信用点 等）
  —— reward 物品图标**无 cls = YOLO gap**
- `确认键`(0.499, 0.807) ← 点这个 dismiss
- `弹窗叉叉`(0.841, 0.132)

★★ **skill 修正**：`扫荡开始`→确认框`确认键`→ **等「掃蕩完成」弹窗 → 点 `确认键`(0.499,0.807) dismiss**
   → 再点左上 `返回键` 退出。（确认框 ≠ 掃蕩完成弹窗，是两个：先消耗确认框，后奖励弹窗）
★ probe 教训：WGC 偶尔截到过渡/stale 帧 → skill 关键节点要 re-detect 不能单帧定论。

→ 下一步（用户指令）：点 `确认键`(0.499,0.807) 关掃蕩完成 → 再 `返回键` 退出

## Step 11 — 掃蕩完成 dismiss → 任務資訊（0票，量态全灰）
帧 2363x1331 · 15 cls · 点 `确认键`(0.499,0.807) → 掃蕩完成关闭，回 任務資訊
- 量态**全灰**：`MIN_灰色`(0.615,0.414)/`减号灰色`(0.669)/`加号灰色`(0.792)/`MAX_灰色`(0.845)
  = 0 票锁 0 → 确认 0 票（DONE 信号，绝不再扫）
- `返回键`(0.044,0.049) top-left = 退出键（用户指认）
→ 点 `返回键`(0.044,0.049) 退出 任務資訊/教室 → 回 Location Select 或 hub

## Step 12 — 连点返回键退回 任務 hub
返回键×3：任務資訊 → 教室关卡列表 → Location Select → **任務 hub**。
（或任意处 `回大厅按钮`(0.964,0.031) 一键回大厅。）

★★ **dot 逻辑再验证（hub 层）**：
- `学院交流会`(0.545,0.803) 上方 `黄点`(0.608,0.765) = **需要去做** ✓
- `悬赏通缉`(0.560,0.548) 旁**已无黄点** = 票用完 done ✓（之前 Step3 时有）
- hub tile **都有 cls**（剧情/任务关卡推图/悬赏通缉/战术大赛/学院交流会/特殊任务）→
  skill 可按 cls 导航 + 各 tile 旁 dot 判断该不该进（无 dot = 该模式今日无事，skip）。

---

# === 学园交流会 (Joint Firing Drill / JFD) 分支 ===
（hub tile 有黄点 = 今日有挑战次数；用户：常规模式非周年活动，可做）

## Step 13 — 进 学院交流会
→ 点 `学院交流会`(0.545, 0.803) 进入

---

## Step 14 — 学院交流会 Academy Select（6/6 票，3 学院分支）
帧 2363x1331 · 11 cls

- **持有票券 6/6**（满）← `学院交流会票` cls(0.053,0.140)=票图标，"6/6" 在其右；
  digit-OCR 读票数，0 → exit（同 bounty 防线①）
- **Academy Select**（選擇學園），3 分支（右侧竖排）：
  - `三一`(0.926,0.253) — 可獲得所有武器強化所需的原料
  - `格黑娜`(0.915,0.401) — 同
  - `千年`(0.928,0.549) — 同
- 都给「武器強化原料」，按需选学院

★ **结构同 bounty**：票数 + 分支选择 + (预期后续 stage list + 扫荡)。
  skill：digit-OCR 票数；分支按 **dashboard 配置**（类似 bounty_branches，建议加 jfd_academy 字段）；
  进分支 → 预期 stage list（得星+入场键 bbox 配对，选最高 → 扫荡 MAX → 确认验"票券"非青辉石）。

→ 下一步（待用户）：进哪个学院（三一/格黑娜/千年）？dashboard 配置项？
★ 用户：**JFD 学院分支也由 dashboard 前端选择**（同 bounty）。本次 = 千年。
  建议 dashboard 加 `jfd_academy` 字段（三一/格黑娜/千年）。

## Step 15 — 进 千年 学院（dashboard 选的）
→ 点 `千年`(0.928, 0.549) 进入

---

## Step 16 — 千年 stage list（關卡目錄，01-04）
帧 2363x1331 · 16 cls

- 持有票券 **6/6**（`学院交流会票` 0.094,0.271）
- 關卡目錄：`01 千年A` / `02 千年B` / `03 千年C` / `04 千年D`（全 3 星）
  - `关卡得星_3`(x≈0.561, y=0.280/0.417/0.555/0.694) + `入场键`(x≈0.873, y=0.257/0.394/0.533/0.671)
- 最高 = **04 千年D**（底部，入场键 0.873,0.671）；4 关全可见（可能无需滚动）

★★ **与 bounty stage list 完全同构** → skill **复用 bounty 选关+扫荡逻辑**：
  `关卡得星_3`+`入场键` bbox 配对 → 滑到底确认(Δ<阈值) → 选最高 `入场键` → 任務資訊
  → `MAX_可点击` 拉满(6票) → `扫荡开始` → 确认框验「票券」非「青辉石」→ `确认键`
  → **掃蕩完成 弹窗 → `确认键`(~0.5,0.81) dismiss** → 票减 → 重复至 0 → 返回键 exit

→ 下一步（待用户）：进 04 千年D（最高 0.873,0.671）应用 bounty 扫荡 pattern？
★ 用户：直接选最底 `入场键`（JFD 千年只 4 关无需滚），进去会见 `MAX_可点击`（6票），
  思路同 bounty，一步步验证。

## Step 17 — 进 04 千年D（最高）
→ 点最底 `入场键`(0.873, 0.671) → 验证 任務資訊 出现 `MAX_可点击`（6票，对比 bounty 1票灰）

实测 ✅：点 04 千年D 入场键 → 任務資訊 弹窗，**`MAX_可点击`(0.845,0.415) 出现**（6票可拉满）！
对比 bounty 1 票时是 `MAX_灰色` → **证实量态 cls 随票数变化**，skill 用 `MAX_可点击`/`MAX_灰色`
判断要不要点 MAX。bounty pattern 在 JFD **完全通用**。
- `MIN_灰色`(0.615,0.415)/`减号灰色`(0.669)（在 min）/ `加号`(0.792)（可+）/
  `扫荡开始`(0.731,0.561) / `任务开始`(0.730,0.745) / `学院交流会票`×2 / `弹窗叉叉`(0.880,0.193)

## Step 18 — 点 MAX 拉满（6票）
→ 点 `MAX_可点击`(0.845, 0.415) → 验证数量变 6、MAX 变灰

实测 ✅：点 MAX → 数量 **6**。量态翻转：`加号灰色`(0.792)+`MAX_灰色`(0.845) 到顶；
`MIN_可点击`(0.615)+`减号`(0.669) 可用。**与 bounty 完全一致**。

成本（04 千年D, 推薦Lv.95）：
- `掃蕩開始`(0.731,0.561)：⚡ **990 → 900**（-90 AP，15/次）+ 票 **6 → 0**（6票）
- `任務開始`(单次)：⚡ 990→975 + 票 6→5

★★ **JFD 掃蕩耗「体力(AP) + 学院交流会票」**（非青辉石 ✅，但**比 bounty 多耗 AP**）！
   skill 需 **digit-OCR 体力**够不够（6 次=90AP）；AP 不足 → 减少 sweep 数量(点减号) 或跳过。
   （bounty 扫荡似乎只耗票不耗 AP；JFD 两者都耗 → skill 两个模式分别处理）
- 獎勵：武器強化素材（x2/x1/x1）

## Step 19 — 掃蕩開始（6次）
→ 点 `扫荡开始`(0.731, 0.561) → 验证确认框（验「学院交流会票券」非青辉石）

实测：点 扫荡开始 → 弹「通知」确认框：
**「要使用 6張學園交流會票券、AP 90 掃蕩 6次 嗎？」**
- `确认键`(0.597,0.696) / `取消键`(0.400,0.702) / `弹窗叉叉`(0.691,0.229)
- 消耗 = **6 學園交流會票券 + 90 AP**（**非青辉石** ✅）

★★ JFD 安全判别：确认框含「學園交流會票券」+「AP」→ 安全 `确认键`；若现「青輝石」→ `取消`。
   （JFD 与 bounty 唯一差异：JFD 多耗 AP；都不碰青辉石）

→ 下一步（用户确认）：点 `确认键`(0.597,0.696) 完成 6 次扫荡

## Step 20 — 確認 → 掃蕩完成 弹窗（probe 又漏截，re-look 抓到）
点 `确认键`(0.597,0.696) → 6 次扫荡 → 量态全灰(票 6→0)。
**首帧又截到 任務資訊(stale)，re-look 才抓到「掃蕩完成」弹窗**（同 bounty 同款）：
- `确认键`(0.499, 0.808) / `弹窗叉叉`(0.841, 0.132)

★★ **再次印证 WGC 过渡帧问题**：扫荡确认后游戏切「掃蕩完成」有动画过渡，单帧易截到旧屏。
   skill：扫荡确认 → **轮询 re-detect 等 `掃蕩完成`/确认键 出现再 dismiss**（别单帧定论）。
★ JFD `掃蕩完成` 与 bounty **完全同款** → 复用同一 dismiss（点 `确认键` 0.499,0.808）。

→ 下一步（用户指令）：点 `确认键`(0.499,0.808) dismiss → 票0 JFD done → 返回键 exit

## Step 21 — 掃蕩完成 dismiss → 任務資訊（票0，JFD done）
点 `确认键`(0.499,0.808) → 回 任務資訊，量态全灰（`MIN/MAX_灰色`+`加号/减号灰色`）= 票 6→0。
JFD done。`返回键`(0.044,0.049) 退出（×N 回 hub，或 `回大厅按钮` 回大厅）。

═══════════════ 学院交流会(JFD) 流程探明（Step 13-21，mirrors bounty）═══════════════
- 入口    : 任務hub → `学院交流会`(0.545,0.803)（dot-gated：有黄点才进）
- 票检查  : digit-OCR `学院交流会票` X/6；0 → exit
- 分支    : dashboard 配置（三一/格黑娜/千年）；建议 dashboard 加 `jfd_academy` 字段
- 选关    : stage list `关卡得星_3`+`入场键` bbox 配对，滑到底选最高 `入场键`（千年只4关01-04，最高04）
- 出击    : 任務資訊 → `MAX_可点击` 在则点 MAX（量态cls随票数变；1票时是 MAX_灰色）→ `扫荡开始`
- ★AP检查 : JFD 掃蕩**耗体力**(~15/次)！digit-OCR 体力够不够（6次=90AP），不够则减数量(减号)或跳
- 确认    : 「要使用 N張學園交流會票券、AP M 掃蕩 N次嗎」→ **验"票券+AP"非"青辉石"** → `确认键`
- 完成    : `掃蕩完成` 弹窗（**WGC 过渡帧易漏截 → re-detect 轮询**）→ `确认键`(0.499,0.808) dismiss
- 收官    : 票=0 → `返回键` exit
- 判别cls : `学院交流会票`/`关卡得星_3`/`入场键`/`扫荡开始`/`任务开始`/`MAX_可点击·灰色`/`MIN_可点击·灰色`/`加号·灰色`/`减号·灰色`
- ⛔ 绝不  : 确认框见青辉石还确认；体力不足硬扫
═══════════════════════════════════════════════════════════════

★★ bounty & JFD 同构 → skill 可抽公共「票券扫荡模式」基类：
   {入口 dot-gate, 票OCR, dashboard分支, stage bbox配对选最高, MAX量态, 扫荡确认验币种, 掃蕩完成re-detect, 票0退出}
   差异参数：票名(悬赏通缉票/学院交流会票) · 是否耗AP(JFD是) · 票上限(6/6)。

## Step 22 — JFD 退出 → 任務 hub（dot 再验证）
`弹窗叉叉`(0.880,0.193) 关 任務資訊 → `返回键`×2（千年stage list → Academy Select → hub）。
**dot 验证（hub）**：
- `学院交流会`(0.545,0.803) 黄点（原 0.608,0.765）**已清除** = JFD 票用完 done ✓
- `战术大赛`(0.666,0.803) 旁 `红点`(0.764,0.766) = arena 有可做（下一候选）
- `悬赏通缉`(0.560,0.548) 无点 = done ✓
→ 待用户：去 战术大赛(arena) / 其他
用户：去 **战术大赛**，用完票 + 领奖。

---

# === 战术大赛 (Arena / PVP) 分支 ===
（arena.py 已存在；本次 probe：用完挑战票 + 领奖；arena 是 PVP 实战，非扫荡；
  同 bounty 买票雷区——绝不花青辉石买票）

## Step 23 — 进 战术大赛
→ 点 `战术大赛`(0.666, 0.803) 进入

---

## Step 24 — 战术大赛 主屏（rank 154, 票 3/5, 3 对手, 2 领奖）
帧 2363x1331 · 14 cls

- 我的排名 **第154名**；**持有票券 3/5**（`战术大赛票` cls 0.055,0.678；digit-OCR X/5）
- 左侧 **两个领奖**：
  - 累積獎勵 16K/1,000K → `领取奖励_黄`(0.275,0.537) ← **可领**（累积竞技场币）
  - 每日獎勵 03:13(计时) → `领取奖励_灰`(0.274,0.646) ← 不可领（计时中）
- 右侧 Season **3 对手**（`战术大赛对战选择区域` cls92）：
  - 第123名 棲夜麗絲(0.667,0.343) / 第126名 Konrad(0.664,0.567) / 第143名 SomSaud(0.662,0.789)
  - 都比我(154)高名次（赢了升名次）；距離更新清單 01:59 / 更新清單
- `對戰記錄` / `防禦編制`（左下）

★ arena = **PVP 实战**（非扫荡，要打或跳过战斗）。
  `领取奖励_黄`/`领取奖励_灰` cls = 可领/不可领判别。
  skill：先领 `领取奖励_黄`（免费累积币）→ 用票挑战对手（票3/5，挑能赢的最低名次）。
  票=0 防买票青辉石（同 bounty 三道防线）。
→ 待用户：先领奖(领取奖励_黄 0.275,0.537) 还是先挑战？挑哪个对手？

★★ 用户定 arena 进入逻辑：
  1. **只要有 `领取奖励_黄` 存在（1 个或 2 个都算），全部点掉**（领完才继续）。
     `领取奖励_灰` = 已领/计时中，**忽略**（不是异常）。
  2. 领完（无 黄）→ 选**最上面**那个 `战术大赛对战选择区域`(第123名, 0.667,0.343) → 准备出击。

## Step 25 — 领取奖励_黄（累积币）
→ 点 `领取奖励_黄`(0.275, 0.537) → 看领奖弹窗/按钮变灰

实测：点 领取奖励_黄 → `获得奖励` 弹窗（`信用点`(0.500,0.505)=累积竞技场币给信用点），
底部 `点击继续字样`(0.517,0.876)。dismiss 点 `点击继续字样`（非中心，同教训）。
→ 点 `点击继续字样`(0.517,0.876) 关闭 → 回 arena 验 领取奖励_黄 消失

实测：点 点击继续字样 → 回 arena，原 `领取奖励_黄`(0.275,0.537) **变 `领取奖励_灰`**(0.275,0.536) = 已领 ✓。
现两个领奖都灰（0.275,0.536 + 0.274,0.646）= 无 黄。
★ **arena 领奖 DONE 信号**：屏上无 `领取奖励_黄`（全 `领取奖励_灰`）→ 领奖完成，转挑战。

## Step 26 — 选最上面对战对手（第123名）
帧 · `战术大赛对战选择区域` ×3：最上 (0.663,0.342)=第123名 / (0.662,0.567) / (0.661,0.789)
→ 点最上 `战术大赛对战选择区域`(0.663, 0.342) → 准备出击

实测：点最上对手 → **對戰對象** 弹窗：
- 對手情報 第117名 Lv.90 ゆり　VS　我的資訊 第154名 Cok11（双方队伍图）
- `攻击编制`(0.500,0.791) 票 **3 → 2**（耗 1 arena 票，**非青辉石** ✅）= 出击按钮
- `弹窗叉叉`(0.791,0.135)；距離更新清單 01:47
- 注：对手显示第117名（列表刷新/重排，无妨）

★ arena 出击入口 = `攻击编制`（设队+出击，耗 1 票）→ 之后 PVP 实战。
→ 下一步（待用户确认）：点 `攻击编制`(0.500,0.791) 出击 → PVP 战斗

## Step 27 — 攻击编制 → 编队屏（出击）
帧 2363x1331 · 11 cls · 点 `攻击编制`(0.500,0.791) → 编队屏
- `出击`(0.913,0.928) ← 出击按钮（右下）
- `快速编辑`(0.939,0.222) 快速编辑队伍 / `1部队高亮`(0.052,0.259) 部队选择（队1激活）
- skill：用预设队伍直接 `出击`（不改编队）
→ 点 `出击`(0.913,0.928) 开打 PVP

## Step 28 — 出击 → AUTO 战斗 → 戰鬥結果 WIN
帧 · 点 `出击`(0.913,0.928) → (确认出击框一闪) → **arena PVP 自动战斗** → 戰鬥結果
- **WIN!** 獎勵 💰 x1000 信用点
- 我的排名 第154名 → **第117名**（赢了升名次）
- 持有票券 3/5 → **2/5**（耗 1 票）
- `确认键`(0.500,0.732) dismiss

★★★ **arena 战斗 = 全自动结算**（无需手动打 / 无需跳过战斗）！
   出击 → 自动战斗(几秒) → 戰鬥結果(WIN/LOSE) → 确认。**skill 大简化**：
   出击 → **轮询等 戰鬥結果/确认键出现** → `确认键` → 回 arena（票 -1，名次更新）→ 重复至票 0。
   （出击后可能有"确认出击耗票"框一闪即过，被自动战斗盖过 → skill 出击后直接轮询结果）
→ 点 `确认键`(0.500,0.732) → 回 arena，验票 2/5，继续下一场

## Step 29 — 達成賽季最高紀錄！弹窗（新最高名次，奖青辉石）
帧 · 点 戰鬥結果 `确认键` → 又弹「達成賽季最高紀錄！」
- **Best Record! 第117名**（第154名 ↑）= 刷新赛季最高名次
- 獎勵 🔷 **x74 = 青辉石 +74**（顶栏 25,082→25,156，**GAIN 非花费** ✅ —— arena 升名次白嫖石头）
- `确认键`(0.500,0.750)

★ arena 刷新赛季最高名次 → 弹此庆祝框（给青辉石奖励），skill 须 dismiss。
  **赢一场可能连弹「戰鬥結果」+「達成最高紀錄」两个**，skill 出击后轮询点掉所有 `确认键` 直到回 arena。
→ 点 `确认键`(0.500,0.750) dismiss → 回 arena 继续（票 2/5）

## Step 30 — 回 arena（rank 117, 票 2/5, 对手刷新）+ ★冷却设计点
帧 · 排名 第117名；票 2/5；对手刷新为更高名次（第92名 2hu / 第104名 Blaze / 第105名 Zishu）；
`领取奖励_黄`(0.275,0.537) 又现（時間獎勵 +80/分 持续累积）。

★★★ **用户提醒：arena 每打完一场有 ~25 秒冷却 `等待時間`**（在 `持有票券` 下方倒计时）！
   skill 必须**出击/挑战前轮询 `等待時間` 倒计时归零**（digit-OCR mm:ss）才能下一场；
   否则连点会失败/无效。probe 因人工慢，冷却自然过了，未触发。
★ 复盘 Step24 左panel 有「等待時間 00:07」即此冷却。

## Step 31 — 第 2 场：选最上对手（第92名）
→ 点最上 `战术大赛对战选择区域`(0.664, 0.345)（聚焦用票；时间奖励黄留末尾/skill每轮可领）

第2场实测（简记，流程同第1场）：攻击编制(0.500,0.791) → 出击(0.913,0.928) → 自动战斗 →
戰鬥結果 `确认键`(0.499,0.642) → 回 arena。**排名仍 117（输了，无 達成最高紀錄 弹窗）**，票 2→1。
★ arena 输赢都消耗票、都回 arena；赢且刷新最高才多弹「達成最高紀錄」。skill 出击后轮询点掉所有
  `确认键`（戰鬥結果 + 可能的 達成最高紀錄），直到回 arena 主屏（见 `战术大赛对战选择区域`）。

## Step 32 — 第 3 场（最后 1 票）：选最上对手（第89名）
帧 · 持有票券 **1/5**；对手刷新 第89名 Johnny / 第103名 Arkan / 第106名 Griven
→ 点最上 `战术大赛对战选择区域`(0.659, 0.344) → 攻击编制 → 出击 → 结算 → 票0 done

第3场实测：攻击编制→出击→自动战斗→戰鬥結果`确认键`(0.500,0.732)→**達成最高紀錄`确认键`(0.500,0.750)**→回arena。
**WIN！排名 117→89**（赢了取代对方名次）；票 1→0。

## Step 33 — arena 票用完（0/5），rank 89，白嫖青辉石
帧 · **持有票券 0/5** ✓（3 票全用）；排名 **第89名**（从154爬65名）；累積量 880/1,000K
- **青辉石 25,082 → 25,224（+142）**：达成最高名次两次给青辉石（+74、+68）= **arena 白嫖石头**
- 还有 `领取奖励_黄`(0.275,0.537) = 時間獎勵 累積（+90/分），可再领

═══════════════ 战术大赛(arena/PVP) 流程探明（Step 23-33）═══════════════
- 入口    : 任務hub → `战术大赛`(0.666,0.803)（dot-gated）
- 领奖    : 进来**点掉所有 `领取奖励_黄`**（`获得奖励`→`点击继续字样`(0.51,0.88)dismiss）；
            `领取奖励_灰`=已领/计时忽略。时間獎勵會持续再变黄（skill 每轮可顺手领）
- 票检查  : digit-OCR `战术大赛票` X/5；0 → 不再挑战（防买票青辉石）
- 挑战    : 选**最上** `战术大赛对战选择区域`(cls92) → 對戰對象 → `攻击编制`(0.500,0.791)
            → 编队屏 `出击`(0.913,0.928)
- ★冷却   : **每场后 ~25s `等待時間` 冷却**（持有票券下方倒计时）→ skill 出击前轮询冷却归零
- 战斗    : **全自动结算**（无需打/跳过）→ `戰鬥結果`(WIN/LOSE)`确认键` →（赢且刷新最高再弹
            `達成賽季最高紀錄``确认键`，白嫖青辉石）→ 出击后**轮询点掉所有`确认键`**直到回arena主屏
- 循环    : 票>0 且冷却好 → 重复挑战（赢了升名次，对手列表刷新）；票=0 → done
- 收益    : 升名次白嫖青辉石(GAIN) + 信用点；`领取奖励_黄`/`领取奖励_灰` = 可领/不可领判别
- ⛔ 绝不  : 票=0 还挑战（弹买票青辉石）；arena 战斗本身不耗青辉石(耗票)
═══════════════════════════════════════════════════════════════
★ arena 与 bounty/JFD 不同：PVP 自动战斗(非扫荡) + 25s冷却 + 升名次白嫖青辉石 + 领奖在主屏。

→ 收尾：点 `领取奖励_黄`(0.275,0.537) 领时间奖励 → 退出

## Step 34 — 时间奖励领取 → 两 领取奖励 全灰（arena 彻底 done）
点 `领取奖励_黄`(0.275,0.537) → `获得奖励`(信用点) → `点击继续字样`(0.519,0.876) dismiss →
回 arena：`领取奖励_灰`(0.275,0.536) + `领取奖励_灰`(0.274,0.646) **全灰**，无 黄。
**战术大赛 daily 全部完成**：票 0/5 + 所有领奖已领 + 排名154→89 + 青辉石+142。

★ arena DONE 信号：① `战术大赛票` 0/5（digit-OCR）② 无 `领取奖励_黄`（全 灰）。

═══════════════ 任務 hub 三模式 probe 完成（bounty/JFD/arena）═══════════════
共性：dot-gated 入口 / digit-OCR 票数(0则skip防买票) / 量态cls(MAX_可点击·灰色) / 确认框验币种非青辉石。
- bounty & JFD：扫荡型（同构，可抽「票券扫荡基类」）。JFD 多耗 AP。
- arena：PVP 自动战斗型，独立。25s冷却 / 升名次白嫖青辉石 / 主屏领奖(领取奖励_黄)。
待 dashboard 配置：bounty_branches(教室等) · jfd_academy(三一/格黑娜/千年)。
═══════════════════════════════════════════════════════════════

## Step 35 — 回 hub dot 三连验证 + 进 特殊任务 观察
返回键 → hub。**dot 三连验证**：`战术大赛` 红点(原0.764,0.766)**已清**✓ / `学院交流会` 无点✓ /
`悬赏通缉` 无点✓（bounty/JFD/arena 三模式 DONE 信号在 hub 全部验证）。

点 `特殊任务`(0.549,0.676) → 特殊任務 屏（内容**无 cls**，仅顶栏被检到）：
- 标题 特殊任務；左上 **活動進行中**：「可從特殊任務獲得 **3倍獎勵**。（還剩 6 天）」
- Request Select 選擇委託，2 委託：
  - `據點防禦` — 可獲得學生經驗值道具（报告书/经验）
  - `信用貨幣回收` — 可獲得大量信用貨幣
  - 两个都带 活動進行中（3x 加成）
- 性质：**AP 消耗型 farming 委託**（非票券 daily，类似 campaign 关卡）；3x 期适合刷经验书/信用点

★★ **训练缺口（用户指出）**：「**活動進行中**」粉色 banner **未训练 → 补 cls**（归 #22/#29）。
   含义 = 该模式**双倍/三倍掉落加成期**。有用：skill 可判"现在收益翻倍 → 优先刷"。
   ★ 特殊任務 委託 tile（據點防禦/信用貨幣回收）也无 cls → 一并补（v6）。
   ★ 用户：不测刷活动（周年异常）；本次仅观察 UI，未跑委託。

→ 待用户：退出特殊任务 / 其他
