# 挖矿 (Mining: MomoTalk + 剧情) — interactive probe log

探测日期 2026-06-01 · 纯-YOLO probe (scripts/probe.py) · ui=v5 · 用户手动遥控

目的：MomoTalk 挖矿（读未读消息领奖）+ 剧情挖矿（读未读剧情领 pyroxene）→ skill。
关联：已有 StoryMining 设计(#8)。挖矿 = 系统性收集未读内容的免费奖励（**青辉石主来源之一**）。

★ 全局教训沿用：ADB丢点重试；GOT_REWARD dismiss 点头部框/点击继续非中心；WGC过渡帧 re-detect。

---

# === MomoTalk 挖矿 ===

## Step 1 — 大厅，点 MomoTalk
帧 2363x1331

| cls | center | conf | 备注 |
|---|---|---|---|
| MomoTalk | (0.117, 0.231) | ~0.97 | 大厅左侧（之前有未读 badge "33"）|

→ 下一步（用户指令）：点 `MomoTalk` (0.117, 0.231) 进入

实测：点 MomoTalk → MomoTalk 屏（粉色 header，close X `弹窗叉叉` 0.875,0.162）：
- 左侧 tab：人物图标(全部) + **「33」badge 图标**(0.132,0.390 = `momotalk学生聊天区域按钮`) = 33 封未读
- 主区：**學生(219)**，排序 `名字`▼ / `≡↑`；列表按 bond ❤ 显示
  - 即將生日的學生：莉央(D-4,❤28) / 莉央(武裝)(D-4,❤2) / 麻白(D-3,❤20)
  - 所有學生：一花(❤20) / 一花(泳裝) …
- 右侧「請選擇學生」；`收藏图标`(0.176,0.363/0.470) = 置顶/收藏学生

★ **MomoTalk 挖矿** = 读 33 个未读消息领 bond/奖励。`momotalk学生聊天区域按钮`(「33」badge tab) 应是未读筛选。
→ 下一步（待用户）：点 "33" 未读 tab(0.132,0.390) / 某学生 ？
用户：去 对话区域。

## MomoTalk Step 2 — 对话区域 = 未讀訊息(33) 列表
帧 2363x1331 · 15 cls · 点 对话区域 tab → `momotalk学生聊天区域已进入`(0.133,0.390)（tab 进入态 cls）
- 筛选 `未讀`▼；标题 **未讀訊息(33)**
- `学生momotalk信息未读` ×5（x≈0.505，y=0.349/0.456/0.563/0.670/0.776）每行 = 有未读的学生 + 未读数 badge：
  - 良舞[2]「老師，現在有空嗎？」/ 莉[1] / 蓮實(泳裝)[1] / 鹿江[1] / 聖亞(泳裝)[1] / 一花(泳裝)[1]…
- 可滚动（共 33 个）；右侧「請選擇學生」

★ **MomoTalk 挖矿 skill**：进对话区域 → 循环 tap `学生momotalk信息未读`(最上) → 读/回消息 → 领 bond/奖励
  → 列表减少 → 滚动 → 直到无 `学生momotalk信息未读` = done。
  cls：`momotalk学生聊天区域按钮`/`已进入`(tab态) · `学生momotalk信息未读`(未读行) · `收藏图标`。
→ 下一步（待用户）：点最上未读学生 良舞[2](0.505,0.349)？
用户：从最上面开始。

## MomoTalk Step 3 — 点学生 → 右侧分屏开对话
帧 2363x1331 · 点 `学生momotalk信息未读`(0.505,0.349 良舞) → **右侧开 良舞 对话**（左列表保留 = 分屏）
- 右侧：「到此已讀」banner + 良舞「老師，現在有空嗎？」「沒空也出來一下吧！」(2 条 = [2] 未读)
- 左侧 良舞 仍显示 [2]（对话刚开，未读未清）
- 右侧对话气泡 / 回复选项 **无 cls**（YOLO gap，待补）

★ 待探：单条对话挖矿动作 —— 读完自动清未读？还是需**回复选择**（reply choice 按钮位置）？
→ 下一步（待用户）：滚动读完 / 回复 / 已读即可？

## MomoTalk Step 4 — 回覆选项出现（`学生信息回复选项` cls）
帧 2363x1331 · 16 cls · 右侧对话底部出 `回覆` 区 + 回复选项「咦？怎麼了嗎？」@ `学生信息回复选项`(0.747,0.617)

★★ **MomoTalk 单条对话挖矿动作明确**：
  开学生对话 → 读消息 → **`学生信息回复选项`** 出现 → 点它回复 → 推进
  → 可能再出消息+回复（多轮）→ 直到对话结束（未读清除 + bond↑/奖励）→ 下一个 `学生momotalk信息未读`。
  `学生信息回复选项` cls 直接定位回复按钮（可能多个选项；通常点任一/最上一个推进即可）。
- 新有用 cls：`学生信息回复选项`（回复按钮）/ `momotalk学生聊天区域已进入`（tab态）/ `学生momotalk信息未读`（未读行）
→ 下一步（待用户）：点 `学生信息回复选项`(0.747,0.617) 回复推进

★★★ 用户口述【MomoTalk 单条对话完整挖矿循环】：
  点 `学生信息回复选项` → **等学生发消息**（可能延迟）→ 出新 `学生信息回复选项` → 再点
  → 循环 … → 直到出 **「前往羁绊剧情」** 按钮 → 点它 **进入羁绊剧情**（羁绊剧情 = 挖矿核心产出，给奖励/pyroxene）。
  skill：轮询 → 有 `学生信息回复选项` 就点 → 出现「前往羁绊剧情」cls 就点进羁绊剧情。

## MomoTalk Step 5 — 点回复选项（循环开始）
→ 点 `学生信息回复选项`(0.747,0.617) → 轮询等下一个回复选项 / 前往羁绊剧情

实测：多轮点 `学生信息回复选项`（位置随对话变：0.617→0.801；每点→学生发新消息→新选项，
有延迟需轮询）→ 最终出 **`前往羁绊剧情`** @ (0.746,0.818)（**有 cls ✓**）。

★★ **MomoTalk 对话循环（确认）**：
  `while 检到 学生信息回复选项: 点它 (轮询等学生发完→新选项)`
  → 出 `前往羁绊剧情` → 点它 **进入羁绊剧情**。
- 关键 cls 全有：`学生信息回复选项`（回复，多轮）/ `前往羁绊剧情`（终点，进剧情）✓

## MomoTalk Step 6 — 前往羁绊剧情 → 进入羁绊剧情
→ 点 `前往羁绊剧情`(0.746,0.818) 进入羁绊剧情

实测：点 `前往羁绊剧情` → 弹窗（`弹窗叉叉` 0.869,0.253）+ `进入羁绊剧情` @ (0.717,0.782)（**有 cls ✓**）
★ 链路：`前往羁绊剧情` →(弹窗)→ `进入羁绊剧情` → 进剧情。两 cls 都有。

## MomoTalk Step 7 — 进入羁绊剧情
→ 点 `进入羁绊剧情`(0.717,0.782) 进入羁绊剧情（剧情挖矿）

实测：点 `进入羁绊剧情` → 加载 → 进入羁绊剧情场景（先 0 cls 加载，后 `剧情menu`(0.940,0.053)）。

---

# === 剧情挖矿 (Story Mining) ===
（羁绊剧情 = MomoTalk 终点；也适用 主线/短篇/支线剧情。挖矿 = 跳过剧情拿完成奖励）

## 剧情 Step A — 剧情区 4 分类（任務hub→剧情）
帧 2363x1331 · 进 任務hub → `剧情`(0.813,0.242) → 劇情 区：
- `主线剧情`(0.365,0.743) 在奇普托斯發生的事件（大 tile，STORY_MAIN 423）
- `短篇剧情`(0.612,0.533) 學生們的另一面（**黄点**，STORY_SHORT 424）
- `支线剧情`(0.835,0.532) 學生們的平凡日常（**黄点**，STORY_SIDE 425）
- 重播(右下) 一起經歷的回憶 = replay（无奖励，**不挖**）
- 黄点在 短篇/支线 = 有未挖章节
★ 剧情挖矿：进分类 → 找 `剧情图标未完成`(430) 章节 → `进入章节`(139) → `剧情menu`→`跳过故事键`→确认 → 领奖 → 下一章
  （`剧情图标已完成`427 = 已挖，跳过）
→ 待用户：进哪个分类演示（短篇/支线 带黄点）？
★ 用户：**每个分类（主线/短篇/支线）都带黄点 = 都有可挖**（主线黄点在大 tile 顶 ~0.444,0.163）。
  skill 据各分类是否有黄点决定挖哪些；逐个分类挖完。先 主线剧情。

## 剧情 Step B — 进 主线剧情
→ 点 `主线剧情`(0.365, 0.743) 进入

实测：进 主线 → 卷/篇 横向卡片：序幕(完成)/1.對策委員會篇(完成)/2.發…(完成)/最終篇(右下)。
篇按时间排，已完成 = `完成` badge，最新未挖 = **`NEW` badge**。

## 剧情 Step C — 左滑找 NEW 篇（主线挖矿入口）
★ 主线挖矿：在卷列表**左滑**（露出更新的卷）→ 找 badge 是 **`new`** 而非 `完成` 的篇 → 进它挖。
→ swipe 左 (0.65,0.40)→(0.30,0.40) 翻卷，找 NEW badge

实测：左滑 2 次翻卷（序幕/1/2 → 3.伊甸園條約篇/4.卡班諾的兔子篇/5.百花繚亂篇，全 `完成`）
→ 第 2 次左滑后出现 **`new` badge @ (0.669,0.537)** = 未挖的篇（5 右侧，部分被右章节面板挡）。

★★ **主线挖矿判别 cls**：篇 badge = **`new`**(未挖) vs **`完成`**(已挖)。
   skill：进主线 → 左滑翻卷 → 找 badge=`new` 的篇 → 进它 → 挖未完成章。`new`/`完成` 均已训练检出 ✓。
→ 待用户：怎么进 New 篇（直接点 new 卡 0.669,0.537 / 先右滑居中）？
用户：直接点 new。

## 剧情 Step D — New 篇(6.聖劇篇) 选中 → 章节列表
帧 2363x1331 · 点 `new`(0.669,0.537) → **选中** 6.流逝時光的聖劇篇（非直接进，是选中显示右侧章节）：
- 第1章 境界彼岸的風景（无标 = 已挖/完成）
- **第2章 與往日訣別 — `黄点`(0.733,0.494) = 未挖章节** ← 目标
- 第3章 獻給不逝之…的召會 — **灰 + 🔒锁定**（前置 第2章 未打）

★★ **章节级判别**：`黄点`=未挖可打 / 灰+🔒=锁定(前置未满,跳过) / 无标=已挖。
   配合篇级 `new`/`完成` → 挖矿定位：找 new 篇 → 进 → 找黄点章 → 挖。
★ 点 `new` 篇 = 选中(右侧出章节)，不是直接进章节；再点黄点章才进。
→ 待用户：点 第2章(未挖, ~0.88,0.494) 进入挖矿
★ 用户：**通过 `黄点` 找位置然后点进去** —— skill 用 黄点 cls 的 y 定位未挖章节行 → 点该行进入。

## 剧情 Step E — 点黄点章(第2章)进入
→ 点 第2章 行(0.88, 0.494，黄点 y 定位) 进入章节

## 剧情 Step F — 第2章 节点列表（章節目錄）
帧 2363x1331 · 进第2章 → 节点列表（每行一个故事节点 + 入场键）：
- 09 ??? — 🔒锁定：`剧情图标未完成`(0.569,0.350) + `入场键没解锁`(0.915,0.351) = 未解锁,跳过
- 08 波塔帕西斯（2）**`new`** — `剧情图标未完成`(0.522,0.498) + `入场键`(0.868,0.497) = **未挖可进** ← 目标
- 07 波塔帕西斯（1）— `剧情图标已完成`(0.474,0.646) + `入场键`(0.820,0.644) = 已挖(可重看)
- 06 迷路的孩子 — `剧情图标已完成`(0.426,0.793) + `入场键`(0.772,0.791) = 已挖

★★ **节点级挖矿判别（cls 全齐）**：
   `剧情图标未完成`+`new` 且 `入场键`(非 `入场键没解锁`) = 未挖可进 → 点其入场键
   `入场键没解锁` = 锁定(前置未满,跳过)；`剧情图标已完成` = 已挖(跳过)
   三级钻取：篇(`new`/`完成`) → 章(`黄点`) → 节点(`剧情图标未完成`+`入场键`)
→ 待用户：点 08 New 节点 `入场键`(0.868,0.497) 进入
★ 用户预期流程：**入场(未完成节点) → 剧情menu → 跳过故事键 → 确认 → 点击继续字样**（同羁绊剧情跳过）。验证。

## 剧情 Step G — 入场 08 节点 → 跳过领奖
→ 点 `入场键`(0.868,0.497) → 剧情场景 → menu→跳过→确认→点击继续（验证）

实测：点 `入场键`(0.868,0.497) → 弹窗 + `进入章节`(0.499,0.718, STORY_ENTER_CHAPTER 139) + `弹窗叉叉`(0.714,0.222)
→ 链路：入场键 →(弹窗)→ 进入章节 → 剧情场景。
→ 点 `进入章节`(0.499,0.718) 进剧情场景

实测验证：进入章节 → 剧情场景(`剧情menu` 0.940,0.053) → `剧情menu` → `跳过故事键`(0.945,0.164)
→ `确认键`(0.598,0.725) → `获得奖励`(0.498,0.221) → 剧情自动续播(`剧情menu` 又现) → 重复。
**= 与羁绊剧情跳过流程一致 ✓**（用户预期对）。

★★★ 用户关键补充【剧情自动播放】：剧情场景**会自动播放/自动推进** —— 不及时 `剧情menu→跳过`
   截停，它就**自动播下一段**。`剧情menu` 反复出现 = 自动续播（非漏步）。
   → skill 要**尽快 menu→跳过** 截停；WGC 高频轮询见 `剧情menu` 立即点 menu→跳过故事键→确认，
   循环到节点剧情播完/回节点图。

═══════════════ 剧情挖矿 (主线/短篇/支线 Story) 完整流程 ═══════════════
- 入口   : 任務hub → `剧情`(0.813,0.242) → 4 分类（主线/短篇/支线/重播）。各分类有黄点=有可挖；重播不挖。
- 篇级   : (主线)卷横向列表，**左滑翻卷** → 找 badge=`new`(未挖) 的篇（vs `完成`已挖）→ 点 new 篇=选中
- 章级   : 选中篇右侧章节列表 → 找带 `黄点` 的未挖章 → **用黄点 y 定位**点该行进入
- 节点级 : 章節目錄节点列表 → 找 `剧情图标未完成`+`new` 且 `入场键`(非`入场键没解锁`) 的节点 → 点其 `入场键`
           → 弹窗 `进入章节`(139) → 进剧情场景
- 跳过   : while `剧情menu` 在(剧情自动播,需尽快截停): `剧情menu`→`跳过故事键`→`确认键`
           → `获得奖励`(多段,每段或终点给) → (点击继续/自动续播) → 重复 → 节点剧情播完回节点图
- 完成   : 节点变 `剧情图标已完成` → 下一未完成节点 → 章/篇/分类逐级做完 → 无 new/黄点 = done
- ⚠️     : 短篇/支线 多为纯剧情(全跳过)；主线节点若含**战斗节点**需另处理(本次 08 全剧情未遇战斗)
- cls全齐: STORY_MAIN/SHORT/SIDE · `new`/`完成`(篇) · `黄点`(章) · `剧情图标未完成/已完成` ·
           `入场键`/`入场键没解锁` · `进入章节`(139) · `剧情menu`(431) · `跳过故事键`(141) ·
           `确认键` · `获得奖励` · `点击继续字样`(142) · `剧情中断退出`(433) · `剧情观看`(434)
═══════════════════════════════════════════════════════════════

★ 用户补充【点击继续字样 之后】：会出现 **`中断`(剧情中断退出 433) / `观看`(剧情观看 434)**。
  挖矿：点 `中断` 退出节点 → 回节点图 move on（`观看` = 继续看，挖矿不用）。
★ 用户补充【短篇=支线】：**短篇剧情挖矿逻辑 = 支线剧情**（同一套：进分类→找未挖章/节点→进入→跳过）。
  主线是三级(篇→章→节点)；短篇/支线 结构更简单（直接章/节点列表），逻辑同。
★ 用户建议【理解 cls】：可去**训练集翻每个 cls 的 bbox 所在 frame** 来理解其作用/外观
  → 列为 skill 实现阶段的辅助步骤（写每个 skill 前，对照 dataset frame 确认 cls 语义/位置）。
  （可用 agent 系统性扫 dataset：每个 cls → 示例 frame，建 cls→frame 索引。）

## 剧情 Step H — 短篇剧情（=支线逻辑）
帧 2363x1331 · 进 `短篇剧情`(0.612,0.533) → 短篇章节 grid（2列×3行 + `右切换`0.974,0.496 翻页）：
- 萬魔殿的赤冬巡迴錄 / 高地人運行日誌 / 檔案浴場 / 無主的禮物 / 綺羅羅和繪里香… / 某個2月19日的紀錄
- 章节卡完成态 cls 本帧未明显检出（待看每卡 new/已完成 小标）
★ **青辉石 25,454 → 25,534（+80）** = 主线 node 08 跳过领奖到账 ✓（剧情节点 ≈80 pyroxene）
★ 短篇 = 支线 逻辑（用户）：进分类 → 找未挖章节(new) → 进入 → 剧情menu→跳过→确认→获得奖励→点击继续→中断 → 下一个
→ 待用户：进哪个短篇章节（带 new 未挖）演示？
★★ 用户定【短篇/支线 找章节逻辑】：在 grid 找带 **`new`** 字样的章节卡 → 没有就 `右切换`(0.974,0.496)
   翻下一页 → 逐页找，直到找到 `new` 章节 → 进它挖。（无 new 页全是已挖，跳过翻页）
   skill：while 当前页无 `new`: 点 右切换；找到 `new` → 点该卡进入。

## 剧情 Step I — 短篇翻页找 new
本页(page1)无 `new` → 点 `右切换`(0.974,0.496) 翻页找 new
→ 点 右切换

实测：page1 无 new → 右切换 → **page2 出 `new`@(0.537,0.225)** = 章节「然而舍監隊仍在奔跑」(右列上)。
「找 new→没有翻页」逻辑验证 ✓。new badge 在卡左上角，卡 = 然而舍監隊仍在奔跑（中心~0.73,0.28）。
→ 点 new 短篇卡(0.73,0.28，用 new badge 定位) 进入

## 剧情 Step 1 — 羁绊剧情 场景播放
帧 2363x1331 · 1 cls · 剧情场景：良舞对话「老師，你每次都跑過來不累嗎？」
- 右上：**AUTO** 按钮 + **MENU** 按钮（`剧情menu` 0.940,0.053）
- 底右 ▼ = 点任意处推进对话
★ **剧情挖矿高效路径**：`剧情menu`(MENU 0.940,0.053) → `跳過` → 确认 → 跳到结尾 → 领完成奖励 → 退出。
  或 AUTO / 点击推进（慢）。skill 优先 MENU→跳过。
- 关联 StoryMining 设计(#8)；主线/活动剧情同理（找未读章→进入→跳过→领奖）。
→ 下一步（待用户）：MENU 跳过 / AUTO / 点击推进？
用户：点 menu 会出跳过键。

## 剧情 Step 2 — MENU → 跳过故事键
帧 · 点 `剧情menu`(0.940,0.053) → **`跳过故事键`** @ (0.945,0.164) 出现（有 cls ✓）
★ 跳过链路：`剧情menu` → `跳过故事键` → (预期确认弹窗) → 跳到结尾领奖。
→ 点 `跳过故事键`(0.945,0.164) 跳过

## 剧情 Step 3 — 跳过确认框
点 `跳过故事键` → 确认框：`确认键`(0.598,0.723) / `取消键`(0.399,0.725) / `弹窗叉叉`(0.703,0.198)
→ 点 `确认键`(0.598,0.723) 确认跳过 → 预期跳到结尾领奖

## 剧情 Step 4 — 跳过确认 → 羈絆劇情獎勵（★80 青辉石！）
帧 2363x1331 · 点 `确认键` → 跳到结尾 → 右侧出 **羈絆劇情獎勵** 面板：
- 河駒風 良舞 ❤6 / EP 04 隊長的溫柔
- **奖励：🔷 x80 青辉石 + 🟥 x1（角色物品/eleph）**
- 顶栏青辉石 **25,374 → 25,454（+80 确认）** ✓✓

★★★ **剧情挖矿核心产出 = 羁绊剧情每话 ≈ 80 青辉石！** 多学生×多话 = 巨量 pyroxene。
   这是 BA 白嫖青辉石最大来源之一 → MomoTalk/剧情挖矿是高价值日常。
- 左侧 良舞 仍 [1]（新消息「不是吧，又跑過來了」= 下一话 EP05，可继续挖同学生）
- 羈絆劇情獎勵 面板无明显 continue cls → 待确认 dismiss 方式
→ 下一步（待用户）：dismiss 奖励面板方式 + 继续（良舞下一话 / 下一个未读学生）？

## 剧情 Step 5 — 奖励 dismiss → 回列表；同学生重复验证
羈絆劇情獎勵 面板 →(tap)→ `获得奖励`(青辉石) → `点击继续字样`(0.510,0.876) dismiss → 回 MomoTalk 未读列表。
点最上 `学生momotalk信息未读`(0.505,0.348) → **良舞下一话(EP05)对话重开，`学生信息回复选项` 就绪** ✓
→ **同学生多话循环成立**：一个学生可能多话，挖到该学生无未读才 move on 下一个。

═══════════════ 挖矿(MomoTalk + 剧情) 完整流程探明 ═══════════════
**MomoTalk 挖矿**：
- 入口：大厅 `MomoTalk`(0.117,0.231)（未读 badge）→ `momotalk学生聊天区域按钮`(对话区域tab) → `已进入` 态
- 列表：`学生momotalk信息未读`(未读学生行 + 未读数)，可滚动（本次 33→递减）
- 单学生循环：点最上 `学生momotalk信息未读` → **while 检到 `学生信息回复选项`: 点它**（轮询等学生发完→新选项，位置随对话变）
  → 出 `前往羁绊剧情` → 点 → 弹窗 `进入羁绊剧情` → 点 → 进剧情

**剧情挖矿**（羁绊/主线/活动同理）：
- 剧情场景 → `剧情menu`(MENU 0.940,0.053) → `跳过故事键`(0.945,0.164) → `确认键`(0.598,0.723)
  → `羈絆劇情獎勵`(≈**80 青辉石!**) →(tap)→ `获得奖励` → `点击继续字样` dismiss → 回 MomoTalk 列表
- 同学生仍有未读 → 重复；无未读 → move on 下一 `学生momotalk信息未读`；**列表空(无 学生momotalk信息未读) → done**

**产出**：每话羁绊剧情 ≈ 80 青辉石 → 33 学生 × 多话 = 巨量 pyroxene（白嫖青辉石最大来源之一）。
**cls 齐全 ✓**：momotalk学生聊天区域按钮/已进入 · 学生momotalk信息未读 · 学生信息回复选项 ·
  前往羁绊剧情 · 进入羁绊剧情 · 剧情menu · 跳过故事键 · 收藏图标 · 获得奖励/点击继续字样
（仅 羈絆劇情獎勵 面板的 continue 用 点击继续字样；剧情场景对话气泡无 cls 但挖矿不需要）
═══════════════════════════════════════════════════════════════
→ 流程已全捕获；剩 33 学生为纯重复 grind

## MomoTalk Step 7 — 第2学生(莉)验证 + ★点击点学问
- 良舞 走完后未读列表去掉它（未讀 34→31），换 莉 测：
- ★★ **打开学生对话的点击点**：点 `学生momotalk信息未读` 的 **badge 中心(x≈0.505) 不稳**
  （点 莉 badge 两次没开）；点**行左侧头像/名字(x≈0.22)** 一次就开 ✓。
  → skill 点 `学生momotalk信息未读` 时用 **bbox 左侧/整行**，别只点右端 badge 数字。
- 莉 对话开 → 出 **2 个 `学生信息回复选项`**(0.747,0.698 / 0.744,0.790)（多选回复，点任一推进）→ 循环同 良舞 ✓
- **第 2 学生循环通用性验证通过**（MomoTalk 挖矿对不同学生一致）。
→ 点 莉 `学生信息回复选项`(0.747,0.698) 继续

═══════════════ ★★ 用户关键规则【发送信息中】(待补 cls) ═══════════════
对话区域若出现 **`发送信息中`** bbox = **学生还在打字/发话中**（消息未发完）。
skill **必须等 `发送信息中` 消失**（学生全部发完）才判断下一步：
  - 检到 `发送信息中` → **wait**（不点回复、不判对话结束、绝不 move on）
  - 无 `发送信息中` 且有 `学生信息回复选项` → 点回复
  - 无 `发送信息中` 且有 `前往羁绊剧情` → 进剧情
  - 无 `发送信息中` 且无 回复选项/前往羁绊剧情 且该学生无 `学生momotalk信息未读` → 该学生挖完 → move on 下一个
防止：学生打字间隙误判"对话结束"而过早 move on / 漏挖。

★★★ **单学生「本次彻底打完」精确判定（用户定稿）**：
  点 `学生信息回复选项` 后，**学生一般会继续讲话**（→ `学生发送信息中` → 新消息 → 新回复选项）。
  所以必须**持续轮询**：
    while (检到 `学生发送信息中` 或 `学生信息回复选项` 或 `前往羁绊剧情`):
        发送信息中 → 等；  回复选项 → 点；  前往羁绊剧情 → 进剧情(走跳过领奖)
  **当稳定到「既无 `学生发送信息中`、也无 `学生信息回复选项`（也无 前往羁绊剧情）」**
  → 该学生本次羁绊剧情**彻底打完** → move on 下一个 `学生momotalk信息未读`。
  ⚠️ 关键：要等"稳定无"（连续几帧都无），别在学生发话间隙的瞬时空窗误判完成。
  （WGC 连续轮询天然适合：连续 N 帧无 发送信息中/回复选项 才判 done。）
★ `学生发送信息中` 是 MomoTalk 挖矿循环的**节拍器**，轮询它来同步学生发话节奏。

═══ ★ cls 名核对（查 ui_classes.py，2026-06-01）═══
- **真名 = `学生发送信息中`**（cls **438**，MOMO_SENDING）≠ 我先前记的"发送信息中"。
  ★ **更正**：它**已训练、能检出**，我 probe 没看到 ≠ recall 差。真因：① probe 是**离散快照**
  （每次 tap+1.2s 抓一帧，断断续续）② `学生发送信息中` **只闪现一瞬**（学生打字窗口），我没截到那刻。
  （28 帧可选补强让它更稳，但非必须；模型本身работает。）
  ★★ **设计启示**：skill 用 **WGC 连续轮询(~55fps)** 能稳抓这种瞬时 cls；离散 probe 会漏 →
  写 skill 时对 `学生发送信息中` 等瞬时态用连续轮询，别用单帧判断。
- 以下我从 probe 检测拿到的名字**已核对正确**（= model.names / ui_classes.py）：
  `学生momotalk信息未读`(439,MOMO_UNREAD) · `学生信息回复选项`(440,MOMO_REPLY_OPT) ·
  `前往羁绊剧情`(441,GOTO_BOND_STORY) · `进入羁绊剧情`(442,ENTER_BOND_STORY) ·
  `跳过故事键`(141,STORY_SKIP) · `点击继续字样`(142,STORY_TAP_CONTINUE) · `剧情menu`(431,STORY_MENU) ·
  `收藏图标`(33,FAVORITE_ICON)
- 另有相关 cls（story mining 用）：`跳过故事键不可用`(432) · `主线/短篇/支线剧情`(423/424/425) ·
  `进入章节`(139) · `剧情图标已完成/未完成`(427/430) · `剧情中断退出`(433)
★★ **已有 skill**：`brain/skills/momo_talk.py` + `story_mining.py`（SKILL_YOLO_MAP 有 MomoTalk/Story 装载）
   → 本 probe = 验证/补全这两个已有 skill，非从零。
═══════════════════════════════════════════════════════════════

★ 羁绊剧情后续：**前往羁绊剧情不会连续触发**——进过一次后，同学生的后续对话只是剧情后续的
  聊天消息，**直接点掉（点回复/推进）即可**，不会再立刻 前往羁绊剧情。
═══════════════════════════════════════════════════════════════
═══════════════════════════════════════════════════════════════
