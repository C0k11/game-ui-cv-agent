# 训练 / 标注缺口清单 — 来自交互 probe (2026-06-01)

> **提醒用户**：下次重训 UI 模型(v6) 前过一遍这份清单 —— 哪些要**新标**、哪些要**补样本**。
> 来源：本日 probe 7 板块（购买青辉石/社交/制造/商店 + bounty/JFD/arena/特殊任务）。

---

## A. 完全未训练 —— 需新建 cls + 标注

1. **`活動進行中`**（粉色 banner）★★ 高价值
   - 出现：任務 hub 各 tile 上 + 特殊任务屏左上
   - 含义：该模式 **双倍/三倍掉落加成期** → skill 可判「现在收益翻倍，优先刷」
   - 来源：_missions_probe_log Step 35

2. **特殊任务 委託 tiles**：`據點防禦`（学生经验书）/ `信用貨幣回收`（信用点）
   - Request Select 屏，整屏无 cls（仅顶栏）

3. **社交主页 cards**：`好友入口` / `幫手入口`
   - （`社團` 有 cls 但本次漏检，见 B-1）

4. **制造屏元素**（_craft_probe_log）：
   - 空槽 `+開始製造`
   - 制造中 `剩餘時間` 计时条
   - 每槽 `立即完成` 蓝按钮（⚠️ skill 绝不点——耗製造券，但需识别以避开/判断状态）

5. **arena `等待時間` 冷却倒计时**（`持有票券` 下方）
   - 需确认 digit-OCR 区域；arena 每场后 ~25s 冷却，skill 轮询它归零才打下一场

6. （可选低优先）reward 弹窗内**物品图标**（掃蕩完成 / 获得奖励 里的 item icon）
   - 领奖只需 `确认键`/`点击继续字样`，物品 icon 非必需，除非要统计产出

---

## B. 训练过但 recall 弱 —— 需补样本 / 重训

1. **`社團` card**（社交主页）— 训练过，本次 conf<0.20 **漏检** → 补卡片立绘多样本/多背景
2. **`跳过战斗`**（cls437，~7 样本欠训）— arena 自动战斗用不上，但 campaign/story 可能需要
3. **region tiles**（夏莱办公室/夏莱居住区/阿拜多斯高中/格黑娜/千年 等，~12 样本）— 已记 #29
4. **`选择购买` / `GOT_REWARD` / `TICKET_ARENA`** 等 — 已记 #22
5. **`学生发送信息中`**（cls **438**，MOMO_SENDING）— 28 帧（_ui_v3_stat `★需补`）。
   ★ **更正**：它**能检出**，probe 没看到是因为离散快照 + 该 cls 只闪现一瞬（非 recall 失败）。
   28 帧可**选择性补强**让瞬时检测更稳，但非必须。skill 用 WGC 连续轮询即可稳抓。
6. **`社團` 等头像/卡片** recall — 同上。

## D. cls 名核对结论（2026-06-01 probe2）
- 我从 probe 检测拿到的名字 = model.names，**已与 ui_classes.py 核对，全部正确**。
- 唯一记错：用户/我口述"发送信息中" → 真名 `学生发送信息中`(438)。
- MomoTalk/剧情 cls 齐全且名字正确：学生momotalk信息未读(439)/学生信息回复选项(440)/
  前往羁绊剧情(441)/进入羁绊剧情(442)/跳过故事键(141)/点击继续字样(142)/剧情menu(431)/收藏图标(33)。
- 已有 skill：`momo_talk.py` + `story_mining.py`（本 probe 用于验证/补全，非从零）。
- 仍未训练（A 类）：活動進行中 / 特殊任务委託tiles / 社交好友·幫手卡 / 制造屏元素 / arena等待時間。

---

## C. 本次验证「工作正常」的有用 cls（无需训练，备查 / skill 直接用）

领取奖励_黄/灰 · MAX_可点击/灰色 · MIN_可点击/灰色 · 加号/减号(·灰色) · 关卡得星_3 ·
战术大赛对战选择区域(cls92) · 悬赏通缉票/学院交流会票/战术大赛票 · 组合包未选择/已选择 ·
信用点商店_已选中 · 全部选择 · 绿勾 · 免费 · 获得奖励 · 点击继续字样 · 扫荡开始 · 任务开始 ·
入场键 · 一次领取黄色/灰色 · 快速制造 · 购买青辉石 · hub tiles(剧情/任务关卡推图/悬赏通缉/战术大赛/学院交流会) · 各底栏入口

→ 量态 cls（可点击/灰色 配对）+ 黄/灰领取态 cls 是本次最大收获：skill 靠它们精准判断
  边界/可领/已领，避免盲点 + 防青辉石。
